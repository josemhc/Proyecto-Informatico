import Appointment from "../models/appointment.model";

export const createAppointment = async (req, res) => {
    try {
        const { userId, dateTime, ultrasoundType, status, reminderSent } = req.body;
        
    } catch (error) {

    }
}